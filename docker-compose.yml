version: '3'
services:
  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - ${PORT_CLIENT}:${PORT_CLIENT}
    depends_on:
      - backend
  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - ${PORT_SERVER}:${PORT_SERVER}
